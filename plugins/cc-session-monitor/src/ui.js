const TERMINAL = ["completed", "failed", "cancelled", "orphaned"];

export function renderMonitorHtml({ mode = "mcp", token = "" } = {}) {
  const adapter = mode === "dashboard" ? dashboardAdapter(token) : mcpAdapter();
  return `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>CC Session Monitor</title>
  <style>
    :root{color-scheme:light dark;font-family:ui-sans-serif,system-ui,-apple-system,sans-serif;--card:#1f2937;--surface:#111827;--muted:#9ca3af;--line:#374151;--accent:#60a5fa;--ok:#34d399;--bad:#f87171;--warn:#fbbf24;--prompt-bg:#12372d;--prompt-line:#3f8f70;--tool-bg:#172d46;--tool-line:#d5a928;--answer-bg:#1f2937;--code-bg:#0f172a}
    .details::-webkit-scrollbar-track{margin-block:2px 4px}
    *{box-sizing:border-box}body{margin:0;background:transparent;color:#f3f4f6}.monitor{max-width:900px;margin:0 auto;padding:10px}.head{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:8px}.title{font-size:13px;font-weight:700}.head-right{display:flex;align-items:center;gap:9px}.meta{font-size:11px;color:var(--muted)}.language{border:1px solid var(--line);border-radius:6px;background:var(--card);color:inherit;padding:3px 6px;font-size:11px}.empty{padding:18px;text-align:center;color:var(--muted);border:1px dashed var(--line);border-radius:10px}.row{position:relative;margin:6px 0;padding:3px 5px;overflow:visible;transition:margin 220ms cubic-bezier(.16,1,.3,1)}.session-card{position:relative;border:1px solid var(--line);background:var(--surface);border-radius:10px;overflow:visible;transition:transform 220ms cubic-bezier(.16,1,.3,1),box-shadow 220ms cubic-bezier(.16,1,.3,1)}.row:hover,.row:focus-within{margin:10px 0 24px}.row:hover .session-card,.row:focus-within .session-card{transform:translateY(-8px);box-shadow:0 8px 20px #0003}.row::after{content:"";display:none;position:absolute;z-index:1;left:2px;right:2px;top:100%;height:28px}.row:hover::after,.row:focus-within::after{display:block}.main{display:grid;grid-template-columns:12px minmax(0,1fr) auto auto auto;gap:9px;align-items:center;padding:9px 10px;cursor:pointer}.dot{width:9px;height:9px;border-radius:50%;background:var(--accent);box-shadow:0 0 0 3px #60a5fa22}.completed .dot{background:var(--ok)}.failed .dot,.orphaned .dot{background:var(--bad)}.cancelled .dot{background:var(--warn)}.summary{font-size:13px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.status,.elapsed{font-size:11px;color:var(--muted);white-space:nowrap}.close{border:0;background:transparent;color:var(--muted);font-size:16px;padding:0 2px;cursor:pointer}.close:hover{color:#fff}.session-id{display:none;position:absolute;z-index:6;left:15px;top:calc(100% + 1px);max-width:calc(100% - 30px);padding:5px 8px;border-radius:6px;background:#030712;color:#d1d5db;font:10px ui-monospace,monospace;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;box-shadow:0 6px 20px #0008}.row:hover .session-id,.row:focus-within .session-id{display:block}.details{display:none;border-top:1px solid var(--line);padding:9px 12px;max-height:360px;overflow:auto;scrollbar-gutter:stable}.row.open .details{display:block}.current{display:none;margin-bottom:7px;padding:6px 8px;border-radius:6px;background:#60a5fa12;color:var(--muted);font-size:11px;white-space:pre-wrap}.current.visible{display:block}.events{display:flex;flex-direction:column;gap:7px}.message{border:1px solid var(--line);border-radius:9px;padding:8px 10px;font-size:12px;line-height:1.55;overflow:hidden}.message-head{display:flex;align-items:center;gap:7px;margin-bottom:5px;cursor:pointer}.message-head .time,.tool-result-head .time{margin-left:auto;color:var(--muted);font-size:10px;font-variant-numeric:tabular-nums}.icon,.icon-spacer{width:16px;height:16px;flex:none}.badge{display:inline-flex;align-items:center;flex:none;gap:4px;padding:1px 7px;border:1px solid var(--line);border-radius:999px;font-size:10px;line-height:18px;white-space:nowrap;word-break:keep-all;writing-mode:horizontal-tb}.message.prompt{background:var(--prompt-bg);border-color:var(--prompt-line)}.message.prompt .message-head{margin-bottom:2px}.message.prompt .icon,.message.prompt .badge{color:#42a27b}.message.prompt .content{cursor:pointer}.message.prompt.collapsed .content{display:-webkit-box;-webkit-box-orient:vertical;-webkit-line-clamp:5;white-space:normal;overflow:hidden;max-height:7.75em}.message.answer{background:var(--answer-bg)}.message.tool-group{background:var(--tool-bg);border-color:#6f91b633}.message.tool-group .message-head{margin-bottom:0;align-items:flex-start}.message.tool-group .badge,.tool-result .badge{border-color:var(--tool-line);color:var(--tool-line)}.message.tool-group .icon{color:var(--tool-line)}.tool-summary{min-width:0;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.tool-group.expanded .tool-summary{white-space:normal;overflow:visible;text-overflow:clip;overflow-wrap:anywhere}.tool-result{display:none;margin-top:8px;padding:8px;border:1px solid var(--tool-line);border-radius:8px;cursor:pointer}.tool-group.expanded .tool-result{display:block}.tool-result-head{display:flex;align-items:center;gap:7px;margin-bottom:5px}.message.error{border-color:#f8717166;background:#7f1d1d22}.message.error .badge{border-color:#f87171;color:var(--bad)}.markdown{min-width:0;overflow-wrap:anywhere}.markdown>:first-child{margin-top:0}.markdown>:last-child{margin-bottom:0}.markdown p{margin:.4em 0}.markdown h1,.markdown h2,.markdown h3{margin:.65em 0 .3em;line-height:1.25}.markdown h1{font-size:1.35em}.markdown h2{font-size:1.2em}.markdown h3{font-size:1.08em}.markdown ul,.markdown ol{margin:.4em 0;padding-left:1.6em}.markdown blockquote{margin:.5em 0;padding-left:.8em;border-left:3px solid var(--line);color:var(--muted)}.markdown code{font-family:ui-monospace,monospace;background:var(--code-bg);border-radius:4px;padding:.1em .3em}.markdown pre{overflow:auto;background:var(--code-bg);padding:8px;border-radius:6px}.markdown pre code{padding:0;background:none}.markdown a{color:var(--accent)}.footer{margin-top:8px;font-size:10px;color:var(--muted)}
    @media(prefers-color-scheme:light){:root{--card:#f8fafc;--surface:#f8fafc;--muted:#64748b;--line:#e2e8f0;--prompt-bg:#eaf8f0;--prompt-line:#9bd6b4;--tool-bg:#eef6ff;--tool-line:#c79400;--answer-bg:#fff;--code-bg:#f1f5f9}body{color:#0f172a}.close:hover{color:#0f172a}.session-id{background:#0f172a;color:#f8fafc}.row:hover .session-card,.row:focus-within .session-card{box-shadow:0 8px 20px #0f172a24}}
  </style>
</head>
<body><div id="app" class="monitor"><div class="head"><div class="title">CC Session Monitor</div><div class="head-right"><div id="meta" class="meta"></div><select id="language" class="language" aria-label="Language"><option value="en">English</option><option value="zh-CN">简体中文</option></select></div></div><div id="rows"></div><div id="empty" class="empty" hidden></div></div>
<script>
${adapter}
const terminal=new Set(${JSON.stringify(TERMINAL)});
const i18n={'zh-CN':{sessions:'个会话',empty:'还没有 Claude Code 任务',close:'关闭并隐藏',connectionFailed:'连接失败',instruction:'提示词',tool:'工具',result:'结果',error:'错误',status:'状态',progress:'进度',heartbeat:'仍在运行',event:'事件',cancelledText:'任务已取消',failedText:'任务执行失败',finishedText:'已完成',thinking:'模型思考中',waitingOutput:'尚无新输出',starting:'启动中',running:'执行中',generating:'生成中',tool_running:'调用工具',waiting_permission:'等待权限',cancelling:'取消中',idle:'等待输入',idle_error:'本轮失败',completed:'已完成',failed:'失败',cancelled:'已取消',orphaned:'失联'},en:{sessions:'sessions',empty:'No Claude Code sessions yet',close:'Dismiss',connectionFailed:'Connection failed',instruction:'Prompt',tool:'Tool',result:'Result',error:'Error',status:'Status',progress:'Progress',heartbeat:'Still running',event:'Event',cancelledText:'Task cancelled',failedText:'Task failed',finishedText:'finished',thinking:'Model is thinking',waitingOutput:'no new output yet',starting:'Starting',running:'Running',generating:'Generating',tool_running:'Using tool',waiting_permission:'Waiting for permission',cancelling:'Cancelling',idle:'Waiting for input',idle_error:'Turn failed',completed:'Completed',failed:'Failed',cancelled:'Cancelled',orphaned:'Disconnected'}};
const hiddenEvents=new Set(['external_session_started','external_turn_completed','external_session_ended','session_initialized','run_started','run_completed','model_status','hook_event']);
let snapshot={revision:0,lastSeq:0,sessions:[]};let openId=null;let busy=false;let language='en';
const views=new Map();const rowsEl=document.getElementById('rows');const emptyEl=document.getElementById('empty');const metaEl=document.getElementById('meta');const languageEl=document.getElementById('language');
function t(key){return (i18n[language]||i18n.en)[key]||key}
function elapsed(s){const end=terminal.has(s.status)&&s.completedAt?Date.parse(s.completedAt):Date.now();const n=Math.max(0,Math.floor((end-Date.parse(s.startedAt))/1000));const h=Math.floor(n/3600),m=Math.floor(n%3600/60),sec=n%60;return h?String(h)+':'+String(m).padStart(2,'0')+':'+String(sec).padStart(2,'0'):String(m).padStart(2,'0')+':'+String(sec).padStart(2,'0')}
function since(value){const n=Math.max(0,Math.floor((Date.now()-Date.parse(value))/1000));const m=Math.floor(n/60),sec=n%60;return String(m).padStart(2,'0')+':'+String(sec).padStart(2,'0')}
function activityText(s){if(s.activeTool)return (s.activeTool||t('tool'))+(s.activeToolSummary?' · '+s.activeToolSummary:'');if(s.status==='generating')return t('thinking')+' · '+since(s.updatedAt)+' · '+t('waitingOutput');if(s.status==='waiting_permission')return t('waiting_permission')+(s.activeToolSummary?' · '+s.activeToolSummary:'');return ''}
function valueText(value){if(value==null)return '';if(typeof value==='string')return value;if(typeof value==='number'||typeof value==='boolean')return String(value);try{return JSON.stringify(value)}catch{return String(value)}}
function toolResultText(d){let value=d.output;if(typeof value==='string'&&value.trim().startsWith('{')){const pathMatch=value.match(/"(?:filePath|file_path|path)"\\s*:\\s*"([^"\\n]+)"/);if(pathMatch)return pathMatch[1];try{value=JSON.parse(value)}catch{}}if(value&&typeof value==='object'){const path=value.filePath||value.file_path||value.path;if(path)return String(path);const message=value.message||value.result||value.text;if(message)return valueText(message)}const text=valueText(value||d.error||'');return text.length>1600?text.slice(0,1600)+'…':text}
function eventText(e){const d=e.data||{};return valueText(d.text||d.prompt||d.output||d.result||d.error||d.summary||d.status||d.tool||'')}
function escapeHtml(value){return valueText(value).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;')}
function inlineMarkdown(value){let text=escapeHtml(value);const tick=String.fromCharCode(96);const codePattern=new RegExp(tick+'([^'+tick+']+)'+tick,'g');text=text.replace(codePattern,'<code>$1</code>');text=text.replace(/\\[([^\\]]+)\\]\\((https?:\\/\\/[^\\s)]+|mailto:[^\\s)]+)\\)/g,'<a href="$2" target="_blank" rel="noopener noreferrer">$1</a>');text=text.replace(/\\*\\*([^*]+)\\*\\*/g,'<strong>$1</strong>').replace(/__([^_]+)__/g,'<strong>$1</strong>');text=text.replace(/(^|[^*])\\*([^*\\n]+)\\*/g,'$1<em>$2</em>').replace(/(^|[^A-Za-z0-9])_([^_\\n]+)_(?![A-Za-z0-9])/g,'$1<em>$2</em>');return text}
function renderMarkdown(value){const newline=String.fromCharCode(10);const lines=valueText(value).replace(/\\r\\n?/g,newline).split(newline);const out=[];let paragraph=[];let list='';let fenced=false;let code=[];const flushParagraph=()=>{if(paragraph.length){out.push('<p>'+paragraph.map(inlineMarkdown).join('<br>')+'</p>');paragraph=[]}};const closeList=()=>{if(list){out.push('</'+list+'>');list=''}};for(const line of lines){if(line.startsWith(String.fromCharCode(96).repeat(3))||line.startsWith('~~~')){flushParagraph();closeList();if(fenced){out.push('<pre><code>'+escapeHtml(code.join(newline))+'</code></pre>');code=[]}fenced=!fenced;continue}if(fenced){code.push(line);continue}if(!line.trim()){flushParagraph();closeList();continue}const heading=line.match(/^(#{1,3})\\s+(.+)$/);if(heading){flushParagraph();closeList();out.push('<h'+heading[1].length+'>'+inlineMarkdown(heading[2])+'</h'+heading[1].length+'>');continue}const bullet=line.match(/^\\s*[-*+]\\s+(.+)$/);const ordered=line.match(/^\\s*\\d+[.)]\\s+(.+)$/);if(bullet||ordered){flushParagraph();const wanted=bullet?'ul':'ol';if(list!==wanted){closeList();out.push('<'+wanted+'>');list=wanted}out.push('<li>'+inlineMarkdown((bullet||ordered)[1])+'</li>');continue}const quote=line.match(/^>\\s?(.*)$/);if(quote){flushParagraph();closeList();out.push('<blockquote>'+inlineMarkdown(quote[1])+'</blockquote>');continue}paragraph.push(line)}if(fenced)out.push('<pre><code>'+escapeHtml(code.join(newline))+'</code></pre>');flushParagraph();closeList();return out.join('')}
const toolIcon='<svg class="icon" viewBox="0 0 24 24" aria-hidden="true"><path d="M14.7 6.2a5 5 0 0 0-6.1 6.1L3.7 17.2a1.5 1.5 0 1 0 2.1 2.1l4.9-4.9a5 5 0 0 0 6.1-6.1l-2.7 2.1-2.1-2.1 2.7-2.1Z" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linejoin="round"/></svg>';
const promptIcon='<svg class="icon" viewBox="0 0 24 24" aria-hidden="true"><circle cx="8" cy="8" r="3" fill="none" stroke="currentColor" stroke-width="1.7"/><path d="M3.5 19c.4-3.1 2-4.8 4.5-4.8s4.1 1.7 4.5 4.8M15 8h5m-5 3h3" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round"/></svg>';
const answerIcon='<svg class="icon" viewBox="0 0 24 24" aria-hidden="true"><path d="M6 3.5h8.5L19 8v12.5H6Z" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linejoin="round"/><path d="M14.5 3.5V8H19M9 12h7M9 15.5h7" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round"/></svg>';
const resultIcon='<svg class="icon" viewBox="0 0 24 24" aria-hidden="true"><path d="M8 4h8M9 4l1 7-3 3h10l-3-3 1-7M12 14v7" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"/><path d="M9.6 7h4.8M9.8 9.5h4.4" fill="none" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/></svg>';
function stamp(e){return new Date(e.timestamp).toLocaleTimeString([],{hour12:false})}
function setMarkdown(el,text){el.innerHTML=renderMarkdown(text)}
function activateOnKeyboard(el,callback){el.tabIndex=0;el.setAttribute('role','button');el.addEventListener('keydown',event=>{if(event.key==='Enter'||event.key===' '){event.preventDefault();callback(event)}})}
function createView(s){const row=document.createElement('div');row.className='row';row.dataset.id=s.sessionId;row.innerHTML='<div class="session-card"><div class="main"><span class="dot"></span><span class="summary"></span><span class="status"></span><span class="elapsed"></span><button class="close" title="Dismiss">×</button></div><div class="details"><div class="current"></div><div class="events"></div></div></div><div class="session-id"></div>';
  const view={row,summary:row.querySelector('.summary'),status:row.querySelector('.status'),elapsed:row.querySelector('.elapsed'),close:row.querySelector('.close'),sessionId:row.querySelector('.session-id'),details:row.querySelector('.details'),current:row.querySelector('.current'),events:row.querySelector('.events'),state:{lastSeq:0,lastOutputText:'',lastOutputEl:null,lastOutputMode:'',heartbeatEl:null,loading:false,tools:new Map(),pendingTools:[],toolCounter:0}};
  row.querySelector('.main').addEventListener('click',async()=>{openId=openId===s.sessionId?null:s.sessionId;syncOpenRows();if(openId)await loadEvents(openId)});
  view.close.addEventListener('click',async event=>{event.stopPropagation();await api('cc_dismiss',{session_id:s.sessionId});if(openId===s.sessionId)openId=null;await refresh()});
  view.sessionId.textContent=s.sessionId;views.set(s.sessionId,view);rowsEl.appendChild(row);return view}
function updateView(s){const view=views.get(s.sessionId)||createView(s);view.row.className='row '+s.status+(openId===s.sessionId?' open':'');view.summary.textContent=s.summary||'Claude Code';view.status.textContent=t(s.status);view.elapsed.textContent=elapsed(s);view.close.hidden=!terminal.has(s.status);view.close.title=t('close');const active=activityText(s);view.current.textContent=active;view.current.classList.toggle('visible',Boolean(active));return view}
function syncOpenRows(){for(const [id,view] of views)view.row.classList.toggle('open',id===openId)}
function localizeChrome(){languageEl.value=language;emptyEl.textContent=t('empty');for(const session of snapshot.sessions)updateView(session);document.querySelectorAll('[data-kind-key]').forEach(el=>{el.textContent=t(el.dataset.kindKey)});document.querySelectorAll('[data-aria-key]').forEach(el=>{el.setAttribute('aria-label',t(el.dataset.ariaKey));el.title=t(el.dataset.ariaKey)})}
function reconcile(){const ids=new Set(snapshot.sessions.map(s=>s.sessionId));for(const [id,view] of views){if(!ids.has(id)){view.row.remove();views.delete(id)}}for(const session of snapshot.sessions)updateView(session);metaEl.textContent=snapshot.sessions.length+' '+t('sessions');emptyEl.hidden=snapshot.sessions.length>0;syncOpenRows();localizeChrome();tickElapsed()}
function tickElapsed(){const byId=new Map(snapshot.sessions.map(s=>[s.sessionId,s]));for(const [id,view] of views){const session=byId.get(id);if(session){view.elapsed.textContent=elapsed(session);const active=activityText(session);view.current.textContent=active;view.current.classList.toggle('visible',Boolean(active))}}}
function createBadge(key){const badge=document.createElement('span');badge.className='badge';badge.dataset.kindKey=key;badge.textContent=t(key);return badge}
function createPrompt(view,e,text){const el=document.createElement('div');el.className='message prompt collapsed';const head=document.createElement('div');head.className='message-head';head.innerHTML=promptIcon;const time=document.createElement('span');time.className='time';time.textContent=stamp(e);head.appendChild(time);const content=document.createElement('div');content.className='content markdown';setMarkdown(content,text);const toggle=()=>el.classList.toggle('collapsed');el.addEventListener('click',toggle);activateOnKeyboard(el,toggle);el.append(head,content);view.events.appendChild(el)}
function createAnswer(view,e,text,mode){const el=document.createElement('div');el.className='message answer';const head=document.createElement('div');head.className='message-head';head.innerHTML=answerIcon;const time=document.createElement('span');time.className='time';time.textContent=stamp(e);head.appendChild(time);const content=document.createElement('div');content.className='content markdown';setMarkdown(content,text);el.append(head,content);view.events.appendChild(el);view.state.lastOutputText=text;view.state.lastOutputEl=el;view.state.lastOutputMode=mode||'message'}
function createTool(view,e){const state=view.state,d=e.data||{};const existing=d.toolUseId&&state.tools.get(d.toolUseId);if(existing){const tool=d.tool||existing.tool;const detail=d.summary&&d.summary!==tool?valueText(d.summary):'';if(detail&&!existing.detail){existing.tool=tool;existing.detail=detail;existing.summary.textContent=tool+' · '+detail}return existing}const id=d.toolUseId||'tool-'+(++state.toolCounter);const tool=d.tool||t('tool');const detail=d.summary&&d.summary!==tool?valueText(d.summary):'';const el=document.createElement('div');el.className='message tool-group';el.dataset.toolId=id;const head=document.createElement('div');head.className='message-head';head.innerHTML=toolIcon;const summary=document.createElement('span');summary.className='tool-summary';summary.textContent=tool+(detail?' · '+detail:'');const time=document.createElement('span');time.className='time';time.textContent=stamp(e);head.append(summary,time);const result=document.createElement('div');result.className='tool-result';result.dataset.ariaKey='result';result.setAttribute('aria-label',t('result'));result.title=t('result');const resultHead=document.createElement('div');resultHead.className='tool-result-head';resultHead.innerHTML=resultIcon;const resultTime=document.createElement('span');resultTime.className='time';resultTime.textContent=stamp(e);resultHead.appendChild(resultTime);const resultContent=document.createElement('div');resultContent.className='markdown';result.append(resultHead,resultContent);const toggle=()=>el.classList.toggle('expanded');head.addEventListener('click',toggle);result.addEventListener('click',event=>{event.stopPropagation();toggle()});activateOnKeyboard(head,toggle);activateOnKeyboard(result,toggle);el.append(head,result);view.events.appendChild(el);const record={id,tool,detail,summary,el,result,resultHead,resultTime,resultContent,completed:false};state.tools.set(id,record);state.pendingTools.push(record);return record}
function completeTool(view,e){const state=view.state,d=e.data||{};let record=d.toolUseId?state.tools.get(d.toolUseId):null;if(!record)record=[...state.pendingTools].reverse().find(item=>!item.completed);if(!record){record=createTool(view,{...e,data:{tool:d.tool||t('tool'),toolUseId:d.toolUseId}})}record.completed=true;state.pendingTools=state.pendingTools.filter(item=>item!==record);if(d.isError)record.el.classList.add('error');record.resultTime.textContent=stamp(e);setMarkdown(record.resultContent,toolResultText(d)||t('finishedText'))}
function createGeneric(view,e,key,text,className){const el=document.createElement('div');el.className='message '+(className||'');const head=document.createElement('div');head.className='message-head';head.appendChild(createBadge(key));const time=document.createElement('span');time.className='time';time.textContent=stamp(e);head.appendChild(time);const content=document.createElement('div');content.className='markdown';setMarkdown(content,text);el.append(head,content);view.events.appendChild(el);return el}
function appendDisplayEvent(view,e){const state=view.state;if(e.type==='run_started')state.heartbeatEl=null;if(hiddenEvents.has(e.type))return;const d=e.data||{},text=eventText(e);if(e.type==='progress_snapshot'){if(state.heartbeatEl){state.heartbeatEl.querySelector('.time').textContent=stamp(e)}else{state.heartbeatEl=createGeneric(view,e,'heartbeat',t('heartbeat'),'')}return}state.heartbeatEl=null;if(['external_message','text_delta','assistant_message'].includes(e.type)){if(!text)return;const mode=e.type==='text_delta'?'stream':'message';if(state.lastOutputEl&&state.lastOutputMode==='stream'){if(mode==='stream')state.lastOutputText+=text;else if(text===state.lastOutputText)return;else if(text.startsWith(state.lastOutputText))state.lastOutputText=text;else{createAnswer(view,e,text,mode);return}setMarkdown(state.lastOutputEl.querySelector('.content'),state.lastOutputText);return}if(text===state.lastOutputText)return;createAnswer(view,e,text,mode);return}state.lastOutputEl=null;state.lastOutputMode='';if(e.type==='external_prompt'){createPrompt(view,e,d.prompt||text||d.summary);return}if(e.type==='tool_started'){createTool(view,e);return}if(e.type==='tool_completed'){completeTool(view,e);return}if(['model_error','run_failed','external_turn_failed'].includes(e.type)){createGeneric(view,e,'error',text||t('failedText'),'error');return}if(e.type==='run_cancelled'){createGeneric(view,e,'status',t('cancelledText'),'');return}if(text)createGeneric(view,e,'event',text,'')}
async function loadEvents(id){const view=views.get(id);if(!view||view.state.loading)return;const state=view.state;state.loading=true;const follow=view.details.scrollTop+view.details.clientHeight>=view.details.scrollHeight-16;const previousTop=view.details.scrollTop;try{const result=await api('cc_get_events',{session_id:id,after_seq:state.lastSeq,limit:500});const events=result.events||[];for(const event of events){appendDisplayEvent(view,event);state.lastSeq=Math.max(state.lastSeq,event.seq||0)}requestAnimationFrame(()=>{if(follow)view.details.scrollTop=view.details.scrollHeight;else view.details.scrollTop=previousTop})}finally{state.loading=false}}
async function refresh(){if(busy)return;busy=true;try{const result=await api('cc_get_sessions',{});snapshot=result.snapshot||result;const nextLanguage=snapshot.settings?.language;if(nextLanguage&&nextLanguage!==language)language=nextLanguage;if(openId&&!snapshot.sessions.some(s=>s.sessionId===openId))openId=null;reconcile();if(openId)await loadEvents(openId)}catch(error){metaEl.textContent=t('connectionFailed')+': '+error.message}finally{busy=false}}
languageEl.addEventListener('change',async()=>{language=languageEl.value;localizeChrome();metaEl.textContent=snapshot.sessions.length+' '+t('sessions');await api('cc_update_settings',{language});await refresh()});
localizeChrome();refresh();setInterval(refresh,1000);setInterval(tickElapsed,1000);
</script></body></html>`;
}

function dashboardAdapter(token) {
  return `const token=${JSON.stringify(token)};
async function api(method,params){const r=await fetch('/api/rpc',{method:'POST',headers:{'content-type':'application/json','authorization':'Bearer '+token},body:JSON.stringify({method,params})});const body=await r.json();if(!r.ok||body.error)throw new Error(body.error?.message||body.error||r.statusText);return body.result}`;
}

function mcpAdapter() {
  return `let bridgeSeq=1;const pending=new Map();
window.addEventListener('message',event=>{const m=event.data;if(!m||m.jsonrpc!=='2.0')return;if(m.id&&pending.has(m.id)){const p=pending.get(m.id);pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result)}});
function bridge(method,params){return new Promise((resolve,reject)=>{const id=bridgeSeq++;pending.set(id,{resolve,reject});parent.postMessage({jsonrpc:'2.0',id,method,params},'*');setTimeout(()=>{if(pending.delete(id))reject(new Error('The host did not respond to the MCP Apps Bridge'))},10000)})}
async function api(method,params){if(window.openai&&typeof window.openai.callTool==='function'){const r=await window.openai.callTool(method,params);return r.structuredContent||JSON.parse(r.content?.[0]?.text||'{}')}const r=await bridge('tools/call',{name:method,arguments:params});return r.structuredContent||JSON.parse(r.content?.[0]?.text||'{}')}
bridge('ui/initialize',{protocolVersion:'2025-06-18',appInfo:{name:'cc-session-monitor',version:'0.1.0'},capabilities:{}}).then(()=>parent.postMessage({jsonrpc:'2.0',method:'ui/notifications/initialized'},'*')).catch(()=>{});`;
}
